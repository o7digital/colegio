<section class="hero-admission section">
  <div class="container split">
    <div>
      <p class="eyebrow">Inscripciones abiertas</p>
      <h1>Proceso de Admisión</h1>
      <p>
        Educación de Calidad a tu Alcance. Formamos líderes con valores universales y pensamiento crítico,
        autónomo y reflexivo.
      </p>
      <div class="actions">
        <a href="/admision" class="btn btn-primary">Inscríbete</a>
        <a href="/contacto" class="btn btn-ghost">Solicitar informes</a>
      </div>
    </div>

    <aside class="hero-panel" aria-label="Resumen institucional">
      <h2>Admisión 2026</h2>
      <ul>
        <li>Certificaciones académicas con enfoque digital.</li>
        <li>Acompañamiento cercano para estudiantes y familias.</li>
        <li>Preparación sólida para el ingreso universitario.</li>
      </ul>
    </aside>
  </div>
</section>

<style>
  .hero-admission {
    background: radial-gradient(circle at 80% 10%, #d7e8ff 0%, #edf4ff 26%, #ffffff 56%);
    border-bottom: 1px solid var(--color-border);
  }

  .eyebrow {
    display: inline-block;
    margin: 0 0 0.7rem;
    background: #e2efff;
    color: var(--color-navy-900);
    border-radius: 999px;
    font-size: 0.85rem;
    font-weight: 650;
    padding: 0.34rem 0.8rem;
  }

  h1 {
    margin: 0;
    line-height: 1.1;
    color: var(--color-navy-900);
    font-size: clamp(2.2rem, 5.2vw, 4rem);
  }

  p {
    margin: 1rem 0 0;
    max-width: 60ch;
    color: var(--color-muted);
    font-size: clamp(1.02rem, 2vw, 1.2rem);
  }

  .actions {
    margin-top: 1.35rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.7rem;
  }

  .hero-panel {
    background: #fff;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-soft);
    padding: clamp(1.4rem, 3vw, 2rem);
  }

  .hero-panel h2 {
    margin: 0;
    color: var(--color-navy-900);
    font-size: 1.4rem;
  }

  .hero-panel ul {
    margin: 1rem 0 0;
    padding-left: 1.1rem;
    color: var(--color-muted);
  }

  .hero-panel li + li {
    margin-top: 0.55rem;
  }
</style>
